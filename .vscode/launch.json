{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "nes",
      "type": "cppdbg",
      "cwd": "${command:cmake.buildDirectory}/bin",
      "request": "launch",
      "program": "${command:cmake.buildDirectory}/bin/nes",
      "args": ["--rom", "roms/smb.nes", "--decoder", "ntsc-gpu"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
